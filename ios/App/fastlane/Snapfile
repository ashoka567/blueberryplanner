devices([
  "iPhone 16 Pro",
  "iPhone 16 Pro Max"
])

languages([
  "en-US"
])

scheme("AppUITests")
workspace("App.xcworkspace")

output_directory("./fastlane/screenshots")

clear_previous_screenshots(true)
